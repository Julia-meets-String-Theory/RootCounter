name: Polytope302

on:
  push:
  pull_request:
  schedule:
    - cron: "0 0 * * *"

jobs:
  Poly302:
    runs-on: [ubuntu-latest]
    steps:
      - uses: actions/checkout@v3
      - name: Install boost
        run: sudo apt install -y libboost-all-dev
      - name: Configure
        run: ./configure
      - name: Install
        run: make install
      - name: Run test
        run: make poly302_test
